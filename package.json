{
	"name": "fullstack-todolist-app",
	"version": "1.0.0",
	"description": "A fullstack Todolist app built using Vue.js, Express.js and MySQL using sequelize",
	"scripts": {
		"heroku-postbuild": "npm run build-client && npm run install-server",
		"start": "node server/src/app.js",
		"build-client": "cd ./client && npm install && npm run build",
		"install-server": "cd ./server && npm install"
	},
	"engines": {
		"node": ">= 4.0.0"
	},
	"cacheDirectories": [
		"client/node_modules",
		"server/node_modules"
	]
}
